# 会话交接 — 传送带指针显示收尾 + 多端口服务器清理（2026-08-25）

> **[2026-08-27 结案更新]** 本文 §4"未解问题"已由新会话复测并定位结案:
> 与转角无关，实为**直线带贴图双向越界**（物品前半身 ~16px + 指针尖 ~8px）导致的
> 跨格瞬态视觉同格。已按用户不变量"每格只显示指针或物品其一"实施 **v7 + v7b 双向
> 格级像素互斥**修复（`beltItemGeom.ts` 纯函数 self/cell/tip 三层判定；v7b 补"指针
> 自身出格"反向路径）。并新增**指针显隐运行日志** `__game.pointerLog()`（环形 300
> 条，记录每次 hide/show 及原因 self/cell/tip/restore）供实测对照。详见
> `doc/implementation-phase-2.md` 第七、八轮记录；§5 清单第 3 步完成、全部改动已提交。
> 第 4 步"第三个问题"即本问题本体，无其他遗留。T2.16（传送带接入设备输入终端的
> 手工操作辅助）经用户确认另行排期，未含在本轮。

> **给新会话**: 本文是上一会话（T2.10 端口轮询 + 传送带指针显示修复迭代）的完整交接。
> 所有改动**未提交**，留在工作区。新会话按 §5 行动清单执行。

---

## 1. 一句话现状

T2.10 端口轮询已完成并验证；传送带"一格一物品"（指针/物品二选一）与"整链红色堵塞"
已定稿实现并部分验证——但用户在**可能过期的 dev 服务器**上于**转角传送带**处仍看到
指针与物品同格，该残留需在确认服务器干净后复测定论（见 §4）。

## 2. 多端口 dev 服务器问题（用户困惑的根源）

- **现象**: `npm run dev` 显示 5173，但 5174/5175/5176/5177 也都能打开应用；不同端口
  可能跑着**不同版本的代码**（历次会话遗留的 vite 实例，测试完没关）。
- **本次已处理**: 已杀掉 5173(PID 38192)/5174(18772)/5175(38680)/5176(42412) 全部
  4 个实例，`netstat -ano | grep -E ":517[3-7]" | grep LISTENING` 确认清零。
  （含用户刚启动的 5173——已告知，新会话重新起即可。）
- **端口已固定（vite.config.ts `strictPort: true`，本轮已改并实测）**:
  dev 服务器**只会用 5173**；端口被占用时第二个实例直接报错
  `Error: Port 5173 is already in use` 退出，不再自动换端口。
  验收脚本默认 BASE_URL 已同步改为 `http://localhost:5173/`。
- **新会话标准流程**:
  1. `npm run dev` —— 显示 5173 即唯一实例；若报"Port 5173 is already in use"
     说明有残留实例: `netstat -ano | findstr ":5173"` 找 PID →
     `taskkill /PID <pid> /F` 杀掉后重跑；
  2. 浏览器打开 `http://localhost:5173/` 并 **Ctrl+Shift+R 硬刷新**（防模块缓存）；
  3. 验收脚本无需再带 BASE_URL 前缀（默认即 5173）。
- **历史教训**: 本会话曾发生"用户在 5173 测到旧现象、我修的代码在 5177 验证"的错位。

## 3. 当前代码状态（全部未提交，13 文件 +275/−210）

### 3.1 已定稿实现（本轮核心成果，均有验证）

| 主题 | 内容 | 主要文件 |
|------|------|---------|
| **指针可见性定稿** | 段上 items 非空（含 entering 行走中）→ 该格指针**立即隐藏**（物品替换箭头，硬切）；空段 → 指针常显、随链相位流动。**无渐隐/无缓动/无占据半径**——历次迭代变体（相位差渐隐、世界距离渐隐、占据半径、显隐缓动）均被用户实测否决（队列前方箭头出现各种形式的 alpha 渐变闪动） | BeltPointerRenderer.ts |
| **物品世界坐标教训** | 指针渲染器曾把物品坐标写成 `pos+dv×progress×格宽`，上行/左行**整偏一格**（幽灵坐标），导致三轮判定失效。已删除该段代码（定稿方案不依赖坐标）；**教训: 复用渲染位置计算必须逐字对照 BeltItemRenderer.straightWorldPos（progress 从入口边起算，180/270 沿负向）** | （已移除） |
| **链级堵塞判定** | 链内任一物品停走（delta=0）→ **整链 blocked**（红色覆盖完整条传送带，含门口格吸入行走期间）。取代旧"队首停走+下游逆流传播"逐段判定 | BeltSystem.ts |
| **排队停格中心** | 下游最近物品是 entering 时，本段队首钳到 STOP_MAX（停自己格中心，不再爬到段尾边界=两格中间）。代价: 饱和门节拍 3s→4s/件 | BeltSystem.ts |
| **注入段首** | 设备/取货口注入改**段首 progress=0**，退役"物品=实体 pointer"全局相位窗口（不同时间创建的传送带全局锁步不符实际玩法） | OutputOps.ts / DepotOps.ts |
| **指针按链相位** | 指针动画相位 = 全局时钟 + chainId 哈希偏移——同链扶梯连续、异链互相独立 | BeltPointerRenderer.ts |
| **端口轮询 T2.10** | 输入指针轮询（满载冻结不重置）+ 输出活跃队列轮转（堵塞移出/恢复追加队尾）；事件带 portIndex；`__game.test('t210')` 三场景 demo | MachineSystem.ts / BuildingComp.ts / main.ts |
| **recentEvents 400** | 环形缓冲 100→400（连续生产 demo 事件量破百会淘汰头部，导致日志覆盘与绝对下标采样漂移） | MachineSystem.ts |

### 3.2 验证状态（改动后全绿）

- 单测: belt-blocked **9**（重写+新增场景5）/ t210-polling **31** / t23 40 / t24 21 /
  t25 45 / t26 48 / t27 **41**（相位窗口退役断言）/ t28 41 / t212 全过（13a/13b 阈值
  校准 4s 门节拍）/ t29 / t1.7 230 / t1.8 37 / t1.9 40；`npx tsc --noEmit` 干净。
- 浏览器（Playwright 脚本）: `verify-t210-browser.mjs` **6/6**、`verify-t212-browser.mjs`
  **8/8**；页面实拍: 直带满载三格各一物品、无指针并存。
- 运行方式见 §2 标准流程；单测命令
  `node --experimental-strip-types scripts/verify-<名>.ts`（Phase 1 脚本需加
  `--experimental-loader ./scripts/ts-loader.mjs`）。

## 4. 未解问题（新会话第一个任务）

**转角传送带的指针残余**（用户最后截图: 转角处指针与物品同格）:

- 用户在 5173（自启服务器）看到，**但当时 4 个服务器并存，无法排除 5173 提供的是
  旧代码**（vite 长驻实例的模块缓存/HMR 时序问题）。
- 已验证范围: 直带（连拍 6 帧 + 录屏）无并存；**转角格未专门验证**。
- 定稿规则是按"段"判定（段上有物品即隐藏整格指针），理论上与转角几何无关——
  若复现，待查方向:
  1. 转角格指针的**弧线扫过范围**（computeCornerTransform）是否在相邻直格有物品时
     仍与相邻物品贴图重叠（转角弧线端点在格边缘，相邻直格物品若也压线，视觉间距
     可能不足）——若是，考虑转角也纳入"相邻物品压线即隐藏"的扩展判定；
  2. `spawnBelt` 的 incomingDirection/isTail 在转角组合下是否导致 maskKind 判定
     与预期不符；
  3. 先用 §2 流程排除陈旧服务器，再写最小复现（取货口+转角+存货口，物品压满）连拍。
- 另一独立残留: 用户提到的"第三个复杂问题"尚未描述（本会话被多端口问题打断）。

## 5. 新会话行动清单（按序）

1. 读本文 + `doc/implementation-phase-2.md` T2.10 实现笔记（含全部五轮迭代记录）。
2. §2 标准流程起服务器（单实例、记端口、硬刷新）。
3. 复测 §4 转角场景: `__game.test('t210')` 场景 C（中带截短）+ 手动搭一个带转角的
   队列场景连拍观察。**复测通过 → 提交全部未提交改动**（建议拆两笔: T2.10 轮询主体
   + 指针/堵塞视觉定稿）；**仍复现 → 按 §4 待查方向逐个排除**。
4. 处理用户要描述的"第三个问题"。

## 6. 关键产物路径

- 录屏（队列前方箭头表现，第五轮定稿验证）:
  `gui-test-screenshots/t210-queue-front-pointer.webm`
- 直带满载实拍（无并存验证）: `gui-test-screenshots/t210-visual-check.png`
- 一键测试: `__game.test('t210')`（约 2 分钟，三场景）; `__game.test('t212')`
- 调试钩子: `__game.portStatus()`（端口连接/堵塞 + 轮询指针/队列）、
  `__game.productionLog()`（事件含 portIndex）、`__game.beltStatus()`

## 7. 本轮被否决方案备忘（避免新会话再走回头路）

| 轮次 | 方案 | 否决原因（用户实测） |
|------|------|-------------------|
| v1 | 按段硬隐藏（items 非空即隐藏） | 队列前方空格箭头周期性消失出现（步进波，当时被读作闪动） |
| v2 | 相位差渐隐 | 相位解耦后失效——物品与指针同格并存（截图） |
| v3 | 世界距离渐隐（内径 0.3） | 物品坐标幽灵 bug（当时未发现）+ 内径 < 贴图半径，指针从物品背后露出（截图） |
| v4 | 占据半径 0.75 + 世界坐标 | 指针从物品上方露出（同幽灵坐标根源，截图） |
| v5 | 占据半径 + alpha 缓动 220ms | "有 alpha 渐变效果的闪动"——用户要零特殊效果 |
| **v6 定稿** | **二值硬切: items 非空即隐藏，空段常显** | （当前版本，直带已验证，转角待复测） |
