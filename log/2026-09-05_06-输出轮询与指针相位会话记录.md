# 会话记录 — 输出端轮询重构与指针相位修复（T2.21 → T2.23，含撤销）

> ⚠️ **历史文档（2026-09-07 状态更新，下会话必读）**: 本记录描述的**指针相位侧
> 已被 T2.28 整体取代**——"领头硬锁 / PointerPhaseClock / 队列停→箭头停 /
> 箭头与物品对齐"这套架构已于 2026-09-07 由用户拍板的 **0/1 统一模型（指针
> 循环装饰层）** 整体退役：指针与物品零耦合（全局时钟纯函数）、接触即消失、
> 带身遮罩、堵塞时照常循环。详见 `doc/实现笔记/T2.28-指针循环装饰层.md` 与
> `log/2026-09-06_01-指针网格对齐修复会话记录.md` §九~§十二（T2.24 网格注入 /
> T2.26 落点保续 / T2.27 连续流 / T2.27-b 空链段首注入 / T2.27-c 接触规则 /
> T2.28 本模型，均在注入与渲染侧多轮迭代后落地）。
> **仍然有效**的部分: §一 R0 与 §二 的输出轮询侧（T2.21 语义至今未动）、§三
> 复盘中的浮点容差教训（已作为独立修复在 T2.24 重做落地）、§五 通用教训、
> §六 验证命令（断言数有更新：test-output-polling 24、diagnose S1~S10）。
> §二 表格中 `BeltPointerPhase.ts` / `BeltPointerRenderer.ts` 两行与 §四 的
> 残差清单**已过时**（见各处内联标注）。
>
> 🎯 **下一会话聚焦输出端轮询修复——入口路线**:
> - **先读**: `log/2026-09-06_01-…会话记录.md` **§十三「指针槽位对齐 + 轮询
>   交替」**——用户 2026-09-07 三图实测的最新症状（指针不在槽位网格上、双带
>   轮询缺 0-1-0/1-0-1 交替）、根因假设与待确认问题都记在那里。
> - **看什么**: 本记录 §一 R0（T2.21 语义与用户快照验收串）+ §三 复盘 + 下方
>   "09-05 之后输出侧的四笔变更"；代码 = `MachineSystem.ts`（updateDepot /
>   pollOutputBelt 调用点 / OUTPUT_EMIT_INTERVAL_TICKS=40 / outputNextEmitTick）、
>   `OutputOps.ts`（slotPlacement / tryEmitToBelt / findReceiverBelt /
>   chainSnapshot / chainDownstreamMin）、`DepotOps.ts`（emitSourceToBelt）、
>   `BuildingComp`（outputPollQueue / outputNextEmitTick）。
>   注意: **spawnBelt 调试钩子会跳过被设备占用的格**——链首因此可能不在端口格
>   上（玩家拖拽链不受影响，端口格有占用豁免），对照测试时先分清两种创建路径。
> - **09-05 之后输出侧的四笔变更**（本记录没写的）: ① T2.24 网格注入——非空链
>   注入落 frac(链领头) 槽位 + downMin 防御钳（物品间距恒整数格）；② T2.26
>   跨段落点保续——落点 = 越界余量而非 0；③ 浮点容差 ±1e-6（吸收/释放/门口）；
>   ④ T2.27-b 最终态——空链注入 = 段首 0 **立即**注入（"等相位再注入"方案
>   因拖慢出货节奏被否，见 T2.27 笔记 T2.27-b）。
> - **回归脚本**: test-output-polling（24）/ verify-t210-polling（39）/
>   verify-t27-output-port（43）/ verify-t212-depot——改动轮询逻辑后四个全跑。

- 时间：2026-09-05 ～ 2026-09-06
- 范围：输出端口轮询语义重定（T2.21）、传送带指针相位修复（T2.22/T2.22a/T2.22b）、
  槽位时钟与出货对齐（T2.23，**已整体撤销**）
- 最终状态：源码停留在 **T2.22b**（指针相位时钟 + 领头硬锁 + 空带续流），输出轮询为
  T2.21 语义（接收传送带按创建序轮询 + 设备级 40-Tick 节拍）。全部 node 验证绿、tsc 干净。
- 本轮所有改动均未提交（工作区相对上次提交 14ed26d 累计 20+ 文件）。
  （⚠️ 2026-09-07 更新：工作区至今仍未提交，且已推进到 T2.28，见顶部横幅。）

---

## 一、问题时间线（用户报告 → 诊断 → 措施 → 结果）

### R0. 输出端口逻辑重定（会话起点，用户需求）
- 需求：输出按**传送带创建顺序**轮询；单带 1-1-1、两带各 1-0-1、三带各 1-0-0；
  动态接入 B 时 A 先出完密集段再转 A-B 交替（用户给了精确快照串）。
- 现状：旧版按端口定义序轮询、每端口每 Tick 1 件（多带=多倍吞吐）、同端口多带饿死、
  残留 beltPhase 相位闸门。
- **实施 T2.21**：`BuildingComp.outputPollQueue` 改存**接收传送带 handle**（创建序）；
  `pollOutputBelt`/`syncOutputBeltQueue`/`collectReceiverBelts` 纯函数；设备级节拍
  `outputNextEmitTick`（40 Tick）；移除 beltPhase 闸门。
- 结果：✅ 用户示例快照串逐字符复现（16/16 纯函数 + t210 39/39 + t27 43/43）。

### R1. 指针闪烁位移（用户实测：延长 B 传送带后 A、B 指针闪烁，队列似停顿/微退）
- 诊断：v11 指针相位（锁队首 + 空链哈希时钟）两处不连续——①模式切换（清空跳哈希、
  注入跳回）②领头插值外推在停走 Tick 边界回跳 ~0.025 格。
- **实施 T2.22**：渲染侧 `PointerPhaseClock`——每链连续相位载体 virt（跟随领头内插
  位移、空带无缝续流、注入不跳变 + ≤10% 带速微漂移收敛）。
- 结果：❌ 用户实测"空带越长箭头越快"。

### R2. 空带箭头速度随链长加快（用户实测）
- 诊断：**相位载体的推进被写进按段循环**——N 段链每帧推进 N 次 = 箭头 N 倍速。
  诊断脚本未抓到的原因：跳变容差 +0.03 太松，N=2~3 的倍速增量藏在容差内。
- **实施 T2.22a**：推进与取相位拆开，每链每帧恰好推进一次；诊断容差收紧到
  `sampleFlow×1.15+0.002`（合法上界），新增 S6 链长无关性断言。
- 结果：✅ 速度正确，但用户实测仍闪烁（现象变了）。

### R3. 队列前方箭头闪烁 + 物品格冒出指针 + 弯折带指针错位（用户实测，附截图）
- 诊断：T2.22 的"保持原位 + ≤10% 微漂移收敛"——漂移收敛任意偏移需数百帧（等效永不
  收敛），箭头相位与物品槽位周期**永久错开** → 箭头在物品旁边/格中央凭空出现消失。
- **实施 T2.22b**：放弃 hold+漂移，改为**逐帧硬锁领头内插位置**（保留停走零回跳的
  内插修复），空链从最后领头位置续流。
- 结果：⚠️ 用户实测仍闪烁（见 R4），**要求撤销到本轮** —— 但注意：撤销发生在 T2.23
  之后，最终停留状态 = T2.22b。

### R4. 撤销 T2.23，回到 T2.22b（用户指示"问题更多了，先撤销回上一轮修改"）
- 见下文"三、已撤销的 T2.23"。
- 结果：✅ 全量 node 验证恢复 T2.22b 数值（t210 39 / t27 43 / t212 53 / 纯函数 16 /
  诊断 0 非法跳变），tsc 干净。**注意：R3 报告的症状（箭头与物品错开、吸收时跳变）
  在 T2.22b 状态下依然存在**——这是用户知情的取舍。

---

## 二、当前保留的实现（工作区状态）

> ⚠️ 2026-09-07: 下表指针侧两行（`BeltPointerPhase.ts` / `BeltPointerRenderer.ts`）
> 已过时——T2.28 起该文件是 `chainVirt` 纯函数（无积分器/无领头锁），渲染器为
> 循环装饰层（接触消失 + 带身遮罩）。输出轮询侧各行仍然有效。

| 文件 | 内容 |
|---|---|
| `src/game/systems/machine/OutputOps.ts` | T2.21：`beltCreationKey`（chainId 创建序）、`collectReceiverBelts`（全部端口候选带）、`syncOutputBeltQueue`、`pollOutputBelt`（布尔 canAccept） |
| `src/game/systems/MachineSystem.ts` | T2.21：设备级节拍 `OUTPUT_EMIT_INTERVAL_TICKS=40` + `outputNextEmitTick`；移除 beltPhase 闸门 |
| `src/game/components/BuildingComp.ts` | `outputPollQueue` = 接收带 handle 队列；`outputNextEmitTick` |
| `src/game/systems/machine/IntakeOps.ts` | T2.10 输入轮询（先到先得排名序）——本轮未动 |
| `src/game/render/BeltPointerPhase.ts` | T2.22b：PointerPhaseClock——每链连续相位载体 virt；领头**硬锁**（内插，停走零回跳）；空链从最后领头位置续流；`reset()` |
| `src/game/render/BeltPointerRenderer.ts` | 相位来源 = PointerPhaseClock；每链每帧推进一次 |
| `src/game/data/buildings.ts` 等 | `createOutputPollQueue` 已删（队列改发现式初始化 `[]`） |

测试/文档：`scripts/test-output-polling.ts`（16 断言）、`scripts/verify-t210-polling.ts`（39）、
`scripts/verify-t27-output-port.ts`（43）、`scripts/diagnose-pointer-flicker.ts`（S1~S7 场景诊断，
当前 0 非法跳变）；`doc/实现笔记/T2.21、T2.22` + Wiki 02/03/04/05/06/11 已同步。

## 三、已撤销的 T2.23（槽位时钟与出货对齐）

**动机**：R3 诊断确认——领头条件在**物品间距为整数格**时精确（全链共享格内相位），
T2.21 轮询发货产生非整数间距（截图实测 4.7/1.7 格）→ 箭头与物品错开随机量 + 吸收换
位跳 (间距 mod 1)。方案：让物品天生落在槽位网格上。

**实现过的内容（已全部撤销）**：
- `BeltSlotClock.ts`：仿真侧每链槽位相位（有物=队首 total mod 1；空链=带速自流；
  全局相位播种同步各链边界）；BeltSystem 末尾收集快照推进。
- 出货门控 `atHeadBoundary`：仅链首槽位边界 Tick 注入（MachineSystem/DepotOps），
  间距恒为整数格 → 指针锁永不跳变、箭头永远对齐（诊断 0 跳变，S7 精确对齐通过）。
- `pollOutputBelt` 三态（accept/skip/**wait**——wait 防止队首带饿死）。
- 浮点容差：吸收/释放判定 `±1e-6`（0.475+0.025=1.4999… 下欺导致门口滞留 1 Tick、
  后车多流 0.025 压缩间距 → 队首切换相位跳 +0.05）。
- 渲染器改读仿真时钟（单一事实来源），删除渲染侧时钟。

**撤销原因**：用户实测"问题更多了"。事后复盘的可疑点（未经验证，仅记录）：
1. 门控改变了发货节奏的手感（首件延迟 ≤40 Tick、边界等待期设备输出被暂缓）；
2. 'wait' 语义让轮询队首带阻塞后续带的服务，异常场景（停走链 skip、链间衔接）行为
   组合复杂，未经浏览器级实测就交付；
3. 改动面过大（时钟+门控+三态+容差一次上线），缺乏中间可回退点。

**若未来重拾此方向的注意点**：
- 吸收/释放浮点容差（±1e-6）是**独立、低风险**的修复，值得单独重做并单独验证；
- 门控的"未知链放行、已见链等边界"语义已在 t212 5a 验证过，可复用；
- `Array.from(iterator)` 预消费迭代器会静默清空后续 for-of（本回合 instrumentation
  事故，30 分钟排查）——诊断/插桩不得改变被测代码的迭代器消费。

## 四、当前状态下的已知遗留（T2.22b 语义的固有残差）

> ⚠️ 2026-09-07: 下述残差 1/2（箭头与物品错开、吸收瞬间网格移位）**已随 T2.28
> 相位解耦整体消解**——箭头不再与物品对齐（接触即消失替代），吸收/跨段不再
> 传导给箭头（virt 为纯函数），本节与"根治方向 = T2.23"的指向均作废（实际
> 落地的是 T2.24 网格注入 + T2.28 解耦）。残差 3/4（跨链衔接瞬态、调试钩子
> 物品离格）属物品侧注入口径的接受例外，仍有效（见 T2.24 笔记）。

1. **箭头与物品不保证精确对齐**：物品间距非整数格（轮询节奏不均）时，箭头网格与各
   物品错开最多 ±0.5 格，微漂移以 ≤10% 带速缓慢收敛（实测收敛很慢，接近不收敛）。
   表现：箭头可能出现在物品紧邻位置（截图现象）。
2. **吸收瞬间的小幅网格移位**：队首被吸收时锁定切换，回退量 = 间距 (mod 1)，非整数
   间距时全链箭头跳 0~0.05 格（≈2.4px，每次吸收一次，即每 2 秒）。
3. 跨链衔接（A 链尾流入 B 链头）物品不对齐 B 链网格（瞬态）。
4. 调试钩子直接 push 的物品不在网格上（非玩家路径）。

上述 1/2 的根治方向即被撤销的 T2.23（间距整数化），见"三"。

## 五、通用教训

- **相位锚与速度锚必须同源**：相位取队首、速度取"任意物品最大位移"时，队首冻结而
  后方压缩的场景会每 Tick 回弹抖动。
- **帧增量断言的容差必须贴物理上界**：宽松容差（+0.03）会把"速度错了 N 倍"漏判成
  "无跳变"，给出虚假绿色。
- **渲染侧不要持有与仿真平行的时钟状态**：双时钟必然漂移；T2.23 的"仿真侧单一事实
  来源"方向正确，撤销的只是当时的门控交付方式。
- **浮点固定步长累加的边界判定必须带容差**：0.475+0.025 < 1.5（下欺 2e-16）这类
  门口滞留会引发连锁的间距压缩与相位跳变。
- **回归脚本也是代码**：三轮修复中测试脚本自身出过 4 类问题（ITEM/辅助函数被误删、
  跨 alpha 采样假阳性、S6 测量窗口混叠、CRLF 匹配失败），改断言前先跑基线。

## 六、复现/验证命令

```bash
npx tsc --noEmit
node --experimental-strip-types scripts/test-output-polling.ts        # 16 断言
node --experimental-strip-types scripts/verify-t210-polling.ts        # 39
node --experimental-strip-types scripts/verify-t27-output-port.ts     # 43
node --experimental-strip-types scripts/diagnose-pointer-flicker.ts   # 0 非法跳变
```

浏览器级：`verify-t210-browser.mjs` / `verify-t212-browser.mjs`（需 dev server，本轮
未跑，交接给用户实测）。
